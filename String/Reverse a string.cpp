class Solution {
public:
    void reverseString(vector<char>& s) {
        int len=s.size()-1;
        int i=0;
        int j=len/2;
        while(i<=ja)
        {
            swap(s[i],s[len]);
            i++;
            len--;
        }

    }
};